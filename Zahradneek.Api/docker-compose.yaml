version: '3'
services:
  db:
    container_name: 'zahradneek-postgres'
    image: postgres:alpine
    restart: always
    ports:
      - '5432:5432'
    env_file:
      - .env
    volumes:
      - .pgdata/:/var/lib/postgresql/data

volumes:
  db: